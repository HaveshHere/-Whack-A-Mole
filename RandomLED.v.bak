module RandomLED (
	input wire CLOCK_50,
	output reg [3:0] LEDR
);
	reg [25:0] counter = 0;
	
	always @(posedge CLOCK_50) begin
		counter <= counter + 1;
		
		if (counter == 50_000_000) begin
			counter <= 0
			LEDR <= 4'b001 << $random % 4;
		end
	end
endmodule
